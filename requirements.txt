# --- Core API & Servidor ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9      # VITAL: Permite subir archivos (PDF) y datos de formulario

# --- Base de Datos ---
motor>=3.3.0                 # Driver Asíncrono para MongoDB/CosmosDB
dnspython>=2.6.0             # Necesario para conexiones 'mongodb+srv://'

# --- Validación y Configuración ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0       # Para validar que el email del candidato sea real
python-dotenv>=1.0.0

# --- Azure Services (OCR) ---
azure-ai-formrecognizer>=3.3.2  # Cliente para leer los PDFs

# --- Inteligencia Artificial (Agentes) ---
crewai>=0.22.5               # Orquestación de Agentes
langchain-groq>=0.0.1        # Conector para usar Groq (Gratis)

# --- Legado (K-Means) ---
# Los mantenemos para no romper el código antiguo de clustering
scikit-learn>=1.4.0
numpy>=1.26.0